{
  "name": "app-da-vsl-scripts",
  "private": true,
  "version": "1.0.0",
  "description": "Ferramentas Node da skill app-da-vsl (extração de cores, contraste, validação, screenshots).",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "empacotar": "bash ./empacotar.sh",
    "sincronizar": "bash ./sincronizar-template.sh"
  },
  "dependencies": {
    "sharp": "^0.33.0",
    "playwright": "^1.48.0"
  }
}
